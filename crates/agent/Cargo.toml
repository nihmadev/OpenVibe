[package]
name = "agent"
version = "1.2.0"
edition = "2021"

[dependencies]
agent-api = { path = "../agent-api" }
llm = { path = "../llm" }
serde_json = "1"
reqwest = { version = "0.13", features = ["json", "http2"] }
tokio = { version = "1", features = ["full"] }
futures = "0.3"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
